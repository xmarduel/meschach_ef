BUILD:

prerequisites: ( (*) means that the lib is modified and build by us)

- libs:

   - libjansson.a

   - libXaPgplot.a - libcpgplot.a - libpgplot.a
   - (*)libvogle.a - (*)libvopl.a
   - (*)libsciplot.a - libplot.a

   - libspooles.a, libBridge.a
   - libamd.a
   - libclapack.a - libcblas.a

   - libslicot.a
   - libscilabcontrol.a

- python:

    - jsonschema (validation of input files -meshes and config- )

- scripts:

   - p6to (vogle ppm to <format>)

- define(s):

  - -DHAVE_X11 -DHAVE_VOGLE -DHAVE_CPGPLOT -DHAVE_LIBSCIPLOT
  - -DHAVE_SILO -DHAVE_SLICOT -DHAVE_SCILAB_CONTROL 

- graphics:

  - fonts path must be given:
    - PGPLOT_FONTS
    - VOGLE_FONTS


Graphics:
--------
  TODO:
    unify cpgplot, vogle/vopl and libsciplot calls (1d)


Params:
-------
  TODO:
    json config file

- gmsh new file format for cube in 3D
- AdvFunc clean
- Bc clean
- setup functions by names in C program




compile first external libs
----------------------------

# plotutils
+++++++++++++
> make

# spooles
++++++++++
> make lib
> cp spooles.a libspooles.a

> cd LinSol
> make lib

# vogle/vopl
+++++++++++++
build in the project

# pgplot
+++++++++
